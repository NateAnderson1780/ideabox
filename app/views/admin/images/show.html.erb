<%= link_to "Images Index", admin_images_path %>

<h1><%= @image.name %> image</h1>

<%= image_tag(@image.url) %><br>

<%= link_to "Edit", edit_admin_image_path(@image) %><br>
<%= link_to "Delete", admin_image_path(@image), method: :delete %></li>